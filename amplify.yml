version: 1
frontend:
  phases:
    preBuild: 
      commands:
        - npm ci --force
    build: 
      commands:
        - npm run clean && npm run build
  artifacts:
    baseDirectory:  public
    files:
      - "**/*"
  cache:
    - node_modules
    - .cache