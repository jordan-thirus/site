version: 1
frontend:
  phases:
    preBuild: 
      commands:
        - cd site 
        - npm ci
    build: 
      commands:
        - npm run build
  artifacts:
    baseDirectory:  public
    files:
      - "**/*"