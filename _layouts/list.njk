<ul>
	{% for post in postslist | reverse %}
		<li>
            <a href="{{ post.url }}">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a>
            <time datetime="{{ post.data.modified |htmlDateString }}">{{ post.data.modified |htmlDateString }}</time>
		</li>
	{% endfor %}
</ul>